<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_enabled="false">
        <shape>

            <!-- 填充 -->
            <solid android:color="#f6c050" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="#f6c050" />
            <!-- 圆角 -->
            <corners android:radius="2dp" />
        </shape>
    </item>

    <item android:state_pressed="true">
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/orange_deep" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/orange_deep" />
            <!-- 圆角 -->
            <corners android:radius="2dp" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/orange_deep" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/orange_deep" />
            <!-- 圆角 -->
            <corners android:radius="2dp" />
        </shape>
    </item>
    <item>
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/orange" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/orange" />
            <!-- 圆角 -->
            <corners android:radius="2dp" />
        </shape>
    </item>

</selector>