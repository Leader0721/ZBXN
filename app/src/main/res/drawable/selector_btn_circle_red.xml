<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true">
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/cancel_dark" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/cancel_dark" />
            <!-- 圆角 -->
            <corners android:radius="45dp" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/cancel_dark" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/cancel_dark" />
            <!-- 圆角 -->
            <corners android:radius="45dp" />
        </shape>
    </item>
    <item>
        <shape>

            <!-- 填充 -->
            <solid android:color="@color/cancel" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/cancel" />
            <!-- 圆角 -->
            <corners android:radius="45dp" />
        </shape>
    </item>
</selector>